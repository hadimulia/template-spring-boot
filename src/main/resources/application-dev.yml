# Dev profile: overrides for local PostgreSQL. Registry + school datasource
# settings live in application.yml with sensible localhost defaults; this file
# only tunes logging.
logging:
  level:
    org.springframework.web: INFO
    com.template.mapper: debug
    com.template.config: debug
    org.springframework.security: INFO
