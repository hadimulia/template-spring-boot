<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.template.mapper.notification.NotificationMapper">

    <resultMap id="responseMap" type="com.template.dto.notification.NotificationResponse">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="message" column="message"/>
        <result property="type" column="type"/>
        <result property="isRead" column="is_read"/>
        <result property="link" column="link"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdDate" column="created_date"/>
    </resultMap>

    <select id="findByUserId" resultMap="responseMap">
        SELECT id, user_id, title, message, type, is_read, link, created_by, created_date
        FROM notifications
        WHERE user_id = #{userId}
        ORDER BY created_date DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM notifications WHERE user_id = #{userId}
    </select>

    <select id="countUnreadByUserId" resultType="int">
        SELECT COUNT(*) FROM notifications WHERE user_id = #{userId} AND is_read = FALSE
    </select>

    <update id="markAsRead">
        UPDATE notifications SET is_read = TRUE WHERE id = #{id} AND user_id = #{userId}
    </update>

    <update id="markAllAsRead">
        UPDATE notifications SET is_read = TRUE WHERE user_id = #{userId} AND is_read = FALSE
    </update>

</mapper>
